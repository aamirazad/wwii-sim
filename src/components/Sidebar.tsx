export default function Sidebar() {
	return (
		// The sidebar should be so ounded that it looks like an island on the side, not touching or comling close to the left or top edgesi
		//
		<div className="fixed top-0 left-0 h-full w-64 border-slate-200 border-r-2 bg-background p-4 dark:border-slate-800 rounded-xl">
			<h2 className="font-bold text-2xl mb-4">Sidebar</h2>
		</div>
	);
}
